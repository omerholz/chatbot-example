name: chatbot-example
runtime:
  name: python
  options:
    virtualenv: venv
description: A Python program to deploy a serverless telegram bot deployed on AWS Lambda using Pulumi.
config:
  pulumi:tags:
    value:
      pulumi:template: serverless-aws-python

